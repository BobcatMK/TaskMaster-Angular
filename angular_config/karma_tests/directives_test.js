// describe("Test directives",function() {
//     beforeEach(angular.mock.module("TaskMasterApp"));

//     beforeEach(angular.mock.inject(function($compile,$rootScope) {
//         $scope = $rootScope;
//         addClassDirective = angular.element("<div add-class-directive>Content</div>");
//         $compile(addClassDirective)($rootScope);
//     }));

//     it("should change class of div element",function() {
//         var evt = $.event('click')
//         $(addClassDirective).trigger(evt)
//     })
// })